# راهنمای نصب و راه‌اندازی دیتابیس

این فایل راهنمای کامل برای نصب و راه‌اندازی دیتابیس سیستم مدیریت فیلم و سریال است.

## 📋 فایل دیتابیس

**فایل اصلی:** `DATABASE_FINAL_COMPLETE.sql`

این فایل شامل تمام جداول، تنظیمات پیش‌فرض و داده‌های اولیه مورد نیاز سیستم است.

## 🚀 مراحل نصب

### روش 1: استفاده از phpMyAdmin

1. وارد phpMyAdmin شوید
2. دیتابیس مورد نظر خود را انتخاب کنید (یا یک دیتابیس جدید ایجاد کنید)
3. به تب "Import" بروید
4. فایل `DATABASE_FINAL_COMPLETE.sql` را انتخاب کنید
5. روی دکمه "Go" کلیک کنید
6. منتظر بمانید تا عملیات کامل شود

### روش 2: استفاده از خط فرمان MySQL

```bash
mysql -u username -p database_name < DATABASE_FINAL_COMPLETE.sql
```

یا:

```bash
mysql -u username -p
USE database_name;
SOURCE DATABASE_FINAL_COMPLETE.sql;
```

## 📊 جداول ایجاد شده

این فایل SQL شامل جداول زیر است:

1. **sp_admins** - مدیریت ادمین‌ها
2. **sp_cats** - دسته‌بندی‌های فیلم/سریال
3. **sp_channels** - کانال‌های تلگرام
4. **sp_episode_qualities** - کیفیت‌های قسمت‌های سریال
5. **sp_files** - فیلم‌ها و سریال‌ها
6. **sp_options** - تنظیمات ربات
7. **sp_orders** - سفارش‌ها و تراکنش‌ها
8. **sp_qualities** - کیفیت‌های فایل‌ها
9. **sp_series_episodes** - قسمت‌های سریال
10. **sp_tickets** - تیکت‌های پشتیبانی
11. **sp_users** - کاربران
12. **sp_user_views** - بازدیدهای کاربران
13. **sp_vip_plans** - پلن‌های اشتراک VIP
14. **sp_webapp_settings** - تنظیمات مینی اپ

## ⚙️ تنظیمات پیش‌فرض

### دسته‌بندی‌ها

20 دسته‌بندی استاندارد به صورت پیش‌فرض اضافه می‌شوند:
- اکشن، ماجراجویی، انیمیشن، کمدی، درام، فانتزی، وحشت، علمی-تخیلی، هیجان‌انگیز، عاشقانه، جنگی، مستند، زندگینامه، جنایی، خانوادگی، تاریخی، موزیکال، معمایی، ورزشی، وسترن

### پلن‌های VIP

3 پلن پیش‌فرض VIP:
- اشتراک 10 روزه: 299,000 تومان
- اشتراک 30 روزه: 429,000 تومان
- اشتراک 90 روزه: 800,000 تومان

### تنظیمات ربات

56 تنظیمات پیش‌فرض برای ربات شامل:
- پیام‌های خوش‌آمدگویی
- دکمه‌های منو
- پیام‌های مختلف سیستم
- و غیره...

### تنظیمات مینی اپ

تنظیمات پیش‌فرض برای مینی اپ شامل:
- رنگ‌بندی Light Mode و Dark Mode
- تنظیمات لوگو
- تنظیمات منو
- تنظیمات صفحه اصلی
- تنظیمات فیلترها

## 🔧 ویژگی‌های فنی

### Charset و Collation

تمام جداول با `utf8mb4` و `utf8mb4_unicode_ci` ایجاد می‌شوند که:
- از emoji پشتیبانی می‌کند
- از کاراکترهای 4 بایتی پشتیبانی می‌کند
- بهترین پشتیبانی از زبان فارسی را دارد

### Index ها

تمام Index های لازم برای بهینه‌سازی کوئری‌ها ایجاد می‌شوند:
- Primary Key برای تمام جداول
- Foreign Key برای روابط بین جداول
- Index برای فیلدهای پر استفاده

## ⚠️ نکات مهم

1. **بکاپ‌گیری**: همیشه قبل از اجرای فایل SQL، از دیتابیس خود بکاپ بگیرید
2. **دسترسی**: مطمئن شوید که کاربر دیتابیس دسترسی کامل (CREATE, ALTER, INSERT) دارد
3. **نسخه MySQL**: این فایل برای MySQL 5.7+ و MariaDB 10.2+ طراحی شده است
4. **زمان اجرا**: بسته به سرعت سرور، اجرای فایل ممکن است چند ثانیه تا چند دقیقه طول بکشد

## 🔄 به‌روزرسانی

اگر قبلاً دیتابیس را نصب کرده‌اید و می‌خواهید به‌روزرسانی کنید:

1. از دیتابیس خود بکاپ بگیرید
2. فایل SQL را بررسی کنید و فقط بخش‌های جدید را اجرا کنید
3. یا از `CREATE TABLE IF NOT EXISTS` استفاده کنید که به صورت خودکار جداول موجود را نادیده می‌گیرد

## ✅ بررسی نصب

پس از اجرای فایل SQL، می‌توانید با اجرای کوئری زیر، تعداد جداول را بررسی کنید:

```sql
SELECT COUNT(*) as table_count 
FROM information_schema.tables 
WHERE table_schema = DATABASE() 
AND table_name LIKE 'sp_%';
```

باید 14 جدول نمایش داده شود.

## 🐛 رفع مشکلات

### خطا: "Table already exists"
- این خطا طبیعی است اگر جداول قبلاً ایجاد شده باشند
- فایل از `CREATE TABLE IF NOT EXISTS` استفاده می‌کند که این خطا را نادیده می‌گیرد

### خطا: "Access denied"
- مطمئن شوید که کاربر دیتابیس دسترسی کامل دارد
- یا از کاربر root استفاده کنید

### خطا: "Unknown collation"
- مطمئن شوید که نسخه MySQL/MariaDB شما از utf8mb4 پشتیبانی می‌کند
- حداقل نسخه: MySQL 5.5.3+ یا MariaDB 10.0.1+

## 📞 پشتیبانی

در صورت بروز مشکل، لطفاً:
1. لاگ خطاها را بررسی کنید
2. نسخه MySQL/MariaDB خود را بررسی کنید
3. دسترسی‌های کاربر دیتابیس را بررسی کنید

---

**نسخه:** 1.0.0  
**تاریخ:** 2024  
**وضعیت:** ✅ آماده استفاده

