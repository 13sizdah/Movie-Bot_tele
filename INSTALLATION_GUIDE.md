# 📋 راهنمای نصب و به‌روزرسانی نهایی

## 🎯 تغییرات این نسخه:

### 1. رفع مشکل دسته‌بندی‌ها ✅
- دکمه دسته‌بندی به منوی اصلی اضافه شد
- مشکل لود نشدن دسته‌بندی‌ها برطرف شد

### 2. حذف قیمت و فروشگاه ✅
- قیمت از پنل ادمین حذف شد
- همه فیلم‌ها به صورت رایگان در نظر گرفته می‌شوند

### 3. مدیریت کانال‌های اجباری از ربات 📢
- مدیریت کانال‌ها از پنل به ربات منتقل شد
- ادمین می‌تواند از طریق ربات کانال‌ها را مدیریت کند

### 4. سیستم فعال/غیرفعال VIP ⚙️
- ادمین می‌تواند سیستم VIP را از پنل فعال/غیرفعال کند

---

## 📦 فایل‌های جدید:

1. **bot_channels_management.php** - مدیریت کانال‌ها در ربات
2. **Database_Update_VIP_Settings.sql** - تنظیمات VIP
3. **Database_Complete_Final.sql** - دیتابیس کامل

---

## 📝 فایل‌های به‌روزرسانی شده:

1. **index.php** - افزودن دسته‌بندی و مدیریت کانال از ربات
2. **initial.php** - حذف قیمت از نمایش فیلم‌ها
3. **btns.php** - اضافه کردن دکمه دسته‌بندی
4. **admin-panel/src/products.php** - حذف ستون قیمت
5. **admin-panel/src/products_action.php** - حذف فیلد قیمت
6. **admin-panel/src/func.php** - قیمت همیشه صفر
7. **admin-panel/src/qualities_management.php** - رفع مشکل نمایش لینک‌ها
8. **admin-panel/src/nav.php** - حذف لینک کانال‌ها از منو
9. **admin-panel/src/header.php** - حذف لینک کانال‌ها از هدر

---

## 📂 فایل‌های حذف شده:

1. ~~admin-panel/channels.php~~ - حذف شد
2. ~~admin-panel/src/channels_management.php~~ - حذف شد

---

## 🚀 مراحل نصب:

### گام 1: آپلود فایل‌ها

```bash
# آپلود فایل‌های جدید
bot_channels_management.php

# آپلود فایل‌های به‌روزرسانی شده
index.php
initial.php
btns.php
admin-panel/src/products.php
admin-panel/src/products_action.php
admin-panel/src/func.php
admin-panel/src/qualities_management.php
admin-panel/src/nav.php
admin-panel/src/header.php
```

### گام 2: اجرای SQL

در phpMyAdmin دیتابیس خود را انتخاب کنید و این فایل را اجرا کنید:

```sql
Database_Update_VIP_Settings.sql
```

یا اگر می‌خواهید دیتابیس را از ابتدا نصب کنید:

```sql
Database_Complete_Final.sql
```

### گام 3: حذف فایل‌های قدیمی

```bash
# حذف این فایل‌ها از سرور
admin-panel/channels.php
admin-panel/src/channels_management.php
```

---

## 🎮 نحوه استفاده:

### مدیریت کانال‌ها در ربات (فقط برای ادمین):

1. در ربات دستور `/channels` یا متن `📢 مدیریت کانال‌ها` را ارسال کنید
2. گزینه مورد نظر را انتخاب کنید:
   - ➕ افزودن کانال
   - ✏️ ویرایش کانال
   - 🗑️ حذف کانال
   - 🔄 تغییر وضعیت
3. دستورات ربات را دنبال کنید

### فعال/غیرفعال کردن VIP:

1. وارد پنل ادمین شوید
2. به قسمت "تنظیمات" بروید
3. گزینه "فعال/غیرفعال کردن سیستم اشتراک ویژه" را پیدا کنید
4. مقدار را به `1` (فعال) یا `0` (غیرفعال) تغییر دهید

---

## ✅ بررسی نهایی:

- [ ] دکمه دسته‌بندی در منوی اصلی نمایش داده می‌شود
- [ ] دسته‌بندی‌ها به درستی لود می‌شوند
- [ ] قیمت در پنل ادمین نمایش داده نمی‌شود
- [ ] دکمه‌های فعال/غیرفعال در پنل به درستی کار می‌کنند
- [ ] ادمین می‌تواند از ربات کانال‌ها را مدیریت کند
- [ ] منوی کانال‌های اجباری از پنل ادمین حذف شده است

---

## 🐛 رفع مشکلات:

### اگر دسته‌بندی‌ها لود نمی‌شود:
- مطمئن شوید فایل `btns.php` به‌روز شده است
- بررسی کنید که دسته‌بندی‌ای در دیتابیس وجود دارد

### اگر ادمین نمی‌تواند کانال‌ها را مدیریت کند:
- شناسه عددی ادمین را در `config.php` بررسی کنید
- مطمئن شوید `bot_channels_management.php` آپلود شده است

### اگر قیمت هنوز نمایش داده می‌شود:
- کش مرورگر را پاک کنید
- فایل‌های پنل ادمین را دوباره آپلود کنید

---

## 📞 پشتیبانی:

در صورت بروز هر مشکلی، فایل‌های لاگ را بررسی کنید و خطاها را گزارش دهید.

