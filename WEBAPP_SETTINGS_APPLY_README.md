# راهنمای اعمال تنظیمات مینی اپ از دیتابیس

این فایل توضیح می‌دهد که چگونه تنظیمات ذخیره شده در دیتابیس در مینی اپ اعمال می‌شوند.

## 📋 تنظیمات اعمال شده

### 1. 🎨 رنگ‌بندی
رنگ‌های تنظیم شده در پنل مدیریت به صورت خودکار در CSS اعمال می‌شوند:

- **رنگ اصلی (Primary Color)**: `--bg-primary`
- **رنگ متن (Text Color)**: `--text-primary`
- **رنگ تاکیدی (Accent Color)**: `--accent`
- **رنگ حاشیه (Border Color)**: `--border-color`
- **رنگ ثانویه (Secondary Color)**: برای متن‌های ثانویه

**فایل:** `web/includes/header.php` - در بخش `<style>` در `<head>`

### 2. 🖼️ لوگو
لوگوی تنظیم شده در navbar نمایش داده می‌شود:

- اگر لوگو تنظیم شده باشد، تصویر لوگو نمایش داده می‌شود
- اگر لوگو تنظیم نشده باشد، آیکون پیش‌فرض نمایش داده می‌شود
- عرض و ارتفاع لوگو از تنظیمات خوانده می‌شود

**فایل:** `web/includes/header.php` - در بخش navbar

### 3. 📋 منوها
منوهای سفارشی در منوی همبرگری نمایش داده می‌شوند:

- منوهای سفارشی (از دیتابیس) در ابتدای منو نمایش داده می‌شوند
- سپس دسته‌بندی‌ها نمایش داده می‌شوند
- هر آیتم منو شامل: آیکون (emoji)، عنوان و لینک

**فایل:** `web/includes/header.php` - در بخش hamburger-menu

### 4. 🏠 صفحه اصلی
تنظیمات صفحه اصلی کنترل می‌کند که چه بخش‌هایی نمایش داده شوند:

- **نمایش محبوب‌ترین‌ها**: اگر فعال باشد، بخش محبوب‌ترین‌ها نمایش داده می‌شود
- **نمایش جدیدترین‌ها**: اگر فعال باشد، بخش جدیدترین‌ها نمایش داده می‌شود
- **نمایش دسته‌بندی‌ها**: اگر فعال باشد، بخش دسته‌بندی‌ها نمایش داده می‌شود

**فایل:** `web/index.php`

### 5. 🔍 فیلترها
فیلترها کنترل می‌کنند که چه محتوایی و با چه محدودیتی نمایش داده شود:

- **فیلتر محبوب‌ترین‌ها**: نمایش محصولات بر اساس تعداد بازدید (نزولی)
- **فیلتر پر بازدیدترین‌ها**: نمایش محصولات با بیشترین بازدید
- **فیلتر جدیدترین‌ها**: نمایش جدیدترین محصولات
- **محدودیت تعداد**: برای هر فیلتر می‌توان تعداد نمایش را تنظیم کرد

**فایل:** `web/index.php`

## 🔧 توابع استفاده شده

### در `web/config.php`:

```php
// دریافت یک تنظیمات
get_webapp_setting($key, $default = '')

// دریافت تمام تنظیمات یک دسته
get_webapp_settings_by_category($category)

// دریافت منوها
get_webapp_menu_items()

// دریافت رنگ‌ها
get_webapp_colors()

// دریافت لوگو
get_webapp_logo()

// بررسی فعال بودن فیلتر
is_filter_enabled($filter_name)

// دریافت محدودیت فیلتر
get_filter_limit($filter_name, $default = 20)
```

## 📝 نحوه کار

### 1. خواندن تنظیمات
هنگامی که صفحه لود می‌شود، توابع در `web/config.php` تنظیمات را از جدول `sp_webapp_settings` می‌خوانند.

### 2. اعمال رنگ‌ها
رنگ‌ها در بخش `<style>` در `header.php` به صورت CSS Variables اعمال می‌شوند:

```css
:root {
    --bg-primary: <?= $background_color ?>;
    --text-primary: <?= $text_color ?>;
    --accent: <?= $accent_color ?>;
    --border-color: <?= $border_color ?>;
}
```

### 3. اعمال لوگو
لوگو در navbar بررسی می‌شود و اگر وجود داشته باشد، نمایش داده می‌شود:

```php
<?php if (!empty($webapp_logo['url'])): ?>
    <img src="<?= $webapp_logo['url'] ?>" ...>
<?php else: ?>
    <i class="fas fa-film"></i>
<?php endif; ?>
```

### 4. اعمال منوها
منوها از JSON خوانده می‌شوند و در منوی همبرگری نمایش داده می‌شوند:

```php
$menu_items = get_webapp_menu_items();
foreach ($menu_items as $item) {
    // نمایش آیتم منو
}
```

### 5. اعمال فیلترها
فیلترها بررسی می‌شوند و فقط اگر فعال باشند، محتوا نمایش داده می‌شود:

```php
if (is_filter_enabled('filter_popular_enabled')) {
    // نمایش محبوب‌ترین‌ها
}
```

## ⚙️ تنظیمات پیش‌فرض

اگر تنظیماتی در دیتابیس وجود نداشته باشد، مقادیر پیش‌فرض استفاده می‌شوند:

- **رنگ‌ها**: مقادیر پیش‌فرض از CSS
- **لوگو**: آیکون پیش‌فرض
- **منوها**: فقط دسته‌بندی‌ها
- **فیلترها**: همه فعال با محدودیت 20

## 🔄 به‌روزرسانی خودکار

تنظیمات به صورت Real-time اعمال می‌شوند:

1. تغییرات در پنل مدیریت ذخیره می‌شوند
2. در صفحه بعدی مینی اپ، تنظیمات جدید اعمال می‌شوند
3. نیازی به کش پاک کردن نیست (مگر اینکه از کش استفاده کنید)

## 🐛 رفع مشکلات

### مشکل: رنگ‌ها اعمال نمی‌شوند
- بررسی کنید که جدول `sp_webapp_settings` ایجاد شده است
- بررسی کنید که رنگ‌ها در دیتابیس ذخیره شده‌اند
- بررسی کنید که CSS Variables به درستی تعریف شده‌اند

### مشکل: لوگو نمایش داده نمی‌شود
- بررسی کنید که آدرس لوگو درست است
- بررسی کنید که فایل لوگو قابل دسترسی است
- بررسی کنید که `logo_url` در دیتابیس ذخیره شده است

### مشکل: منوها نمایش داده نمی‌شوند
- بررسی کنید که JSON معتبر است
- بررسی کنید که `menu_items` در دیتابیس ذخیره شده است
- بررسی کنید که تابع `get_webapp_menu_items()` به درستی کار می‌کند

### مشکل: فیلترها کار نمی‌کنند
- بررسی کنید که فیلترها در دیتابیس فعال هستند
- بررسی کنید که محدودیت‌ها به درستی تنظیم شده‌اند
- بررسی کنید که کوئری‌های SQL به درستی اجرا می‌شوند

## ✅ چک‌لیست

- [ ] جدول `sp_webapp_settings` ایجاد شده است
- [ ] توابع در `web/config.php` اضافه شده‌اند
- [ ] رنگ‌ها در `header.php` اعمال شده‌اند
- [ ] لوگو در navbar اعمال شده است
- [ ] منوها در منوی همبرگری اعمال شده‌اند
- [ ] فیلترها در `index.php` اعمال شده‌اند
- [ ] تنظیمات صفحه اصلی اعمال شده‌اند
- [ ] تمام تنظیمات از دیتابیس خوانده می‌شوند

---

**تاریخ ایجاد:** 2024
**نسخه:** 1.0.0

